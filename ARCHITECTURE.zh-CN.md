# 多脚本架构

本文记录多脚本架构边界。当前已实现 `TMUX.sh`、`ZSH.sh` 和薄启动器 `dot.sh`；
其他安装器及内置小工具按后续需要再增加。

## 一个安装项，一个独立脚本

```text
dot.sh                # TUI 启动器，后续可加入小工具
TMUX.sh               # 独立 tmux 安装脚本（当前已有）
ZSH.sh                # 独立 zsh 安装脚本（当前已有）
SSH.sh                # 未来的独立 ssh 安装脚本
README.md
README.zh-CN.md
tests/
```

每个安装脚本本身就是完整产品，必须满足：

- 不经过 `dot.sh` 也能直接运行；
- 不 `source`、不调用、也不依赖仓库里的其他安装脚本；
- 自己检测并处理所需工具和软件包；
- 自己负责交互、计划、执行、验证、备份与恢复；
- 可以安全地反复运行，并拥有独立测试；
- 用正确的退出码报告成功或失败。

脚本之间可以复制少量成熟的框架代码，但不能形成运行时耦合。如果 `TMUX.sh`
和 `ZSH.sh` 都需要 Git，就各自检查 Git。重复检查是有意设计：幂等检查成本很低，
而隐藏依赖会让单独下载某个脚本变得不可靠。

项目不设计依赖图、依赖字段、拓扑排序或隐式安装顺序。用户同时选择 tmux 和
zsh，只表示依次运行两个互相独立的安装脚本，不表示组成一个统一事务。

## `dot.sh`

`dot.sh` 是一个很薄的调度器，不是新的安装框架。注册表保持为：

```bash
register_script tmux "TMUX.sh" "安装并配置 tmux"
register_script zsh  "ZSH.sh"  "安装并配置 zsh"
register_tool proxy  "快速配置代理"
```

这里刻意没有依赖字段。启动器将负责：

1. 用 TUI 展示所有已注册的安装脚本；
2. 允许选择一个或多个项目；
3. 只下载用户选中的独立脚本；
4. 至少先用 `bash -n` 检查临时文件；
5. 再用 `bash` 依次执行；
6. 某个脚本失败后继续处理不相关的选择，最后逐项汇总；
7. 清理临时文件。

它不合并各脚本的计划、不共享可变安装状态、不解析依赖，也不承诺多个脚本之间
可以整体回滚。

## 每次获取最新 `master`

`dot.sh` 每次启动都跟随最新 `master`，不把版本固定与可重复构建当作目标。但如果
运行过程中 `master` 恰好发生变化，不能让前后两个安装脚本来自不同版本。因此推荐：
启动时只解析一次 `master` 当前的提交 SHA，本次所选脚本都从该 SHA 下载：

```text
https://raw.githubusercontent.com/51hhh/dot/<本次解析出的-sha>/TMUX.sh
```

这仍然是“每次运行都拉最新主分支”，同时保证单次运行使用同一个完整快照。脚本先
下载到 `mktemp` 创建的安全临时文件，检查后再执行，不能把未经检查的网络响应直接
流进 `bash`。

跟随最新 `master` 就表示信任主分支上的当前代码。需要审计时，应先下载并阅读，
确认后再运行。

## `dot.sh` 内置的小工具

小到不值得单独做安装脚本的功能可以直接集成，例如：

- 给本次选中的安装脚本临时注入代理环境变量；
- 输出配置或清除当前 shell 代理的命令；
- 做简单的网络连通性诊断。

这类工具必须保持短小、可逆、低风险。一旦涉及持久化状态、破坏性动作、大量平台
分支，或者复杂到需要独立测试套件，就应升级成一个独立脚本。

代理还有一个必须明确的进程边界：`bash dot.sh` 是子进程，不能把环境变量导出到
父 shell。未来只能选择以下真实语义之一：代理只作用于子安装脚本；输出命令，由
用户明确执行 `eval "$(dot.sh proxy ...)"`；或经明确确认后写入 shell 配置。不能声称
运行一次子脚本就永久修改了调用者的 shell。

## `dot.sh` 的测试

- 注册表与菜单渲染；
- 选择项到脚本文件名的精确映射；
- 只下载被选中的脚本，绝不自动补依赖；
- 每次运行只解析一次最新 `master` 的 SHA；
- 执行前确实做语法检查；
- 下载失败和子脚本失败都进入最终汇总；
- 临时文件会被清理；
- 代理输出、子进程作用域和清除行为正确；
- 单元测试替换下载函数，不访问真实网络；
- CI 可另设一个真实 raw GitHub 链接的冒烟测试。
